#include <iostream>

using namespace std;

int main()
{

    // auto a;

    // a = 3;

    // auto x = 4;
    // auto y = 3.37;
    // auto ptr = &x;
    // cout << typeid(x).name() << endl
    //      << typeid(y).name() << endl
    //      << typeid(ptr).name() << endl;

    // we have used typeid to get the type of the variable

    return 0;
}
